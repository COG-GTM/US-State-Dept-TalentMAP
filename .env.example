# Copy this file to .env and fill in real values. Never commit .env.

# Secret key for the Django API container (docker-compose.yml)
DJANGO_SECRET_KEY=change-me

# Optional: allowed CORS origin for the middle-tier server
# ALLOWED_ORIGIN=https://example.gov
